export type {{pascalCase name}} = {
  id: number;
  name: string;
  createdAt: string;
  updatedAt: string;
};

export type {{pascalCase name}}CreatePayload = {
  name: string;
};

export type {{pascalCase name}}UpdatePayload = {
  name: string;
};

export interface {{pascalCase name}}PaginatedResponse {
  data: {{pascalCase name}}[];
  meta: {
    page: number;
    limit: number;
    totalData: number;
    totalPages: number;
  };
}