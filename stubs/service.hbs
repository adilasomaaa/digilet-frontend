import { http } from "../lib/fetcher";
import type {
  {{pascalCase name}},
  {{pascalCase name}}CreatePayload,
  {{pascalCase name}}PaginatedResponse,
  {{pascalCase name}}UpdatePayload,
} from "@/models";

export const {{camelCase name}}Service = {
  async index(params?: any) {
    return await http<{{pascalCase name}}PaginatedResponse>("{{kebabCase name}}", {
      method: "GET",
      query: params,
      auth: true,
    });
  },

  async create(payload: {{pascalCase name}}CreatePayload) {
    return await http<{ data: {{pascalCase name}} }>("{{kebabCase name}}", {
      method: "POST",
      auth: true,
      body: payload,
    });
  },

  async show(id: number) {
    return await http<{ data: {{pascalCase name}} }>(`{{kebabCase name}}/${id}`, {
      method: "GET",
      auth: true,
    });
  },

  async update(id: number, payload: {{pascalCase name}}UpdatePayload) {
    return await http<{ data: {{pascalCase name}} }>(`{{kebabCase name}}/${id}`, {
      method: "PATCH",
      auth: true,
      body: payload,
    });
  },

  async delete(id: number) {
    return await http<{ data: {{pascalCase name}} }>(`{{kebabCase name}}/${id}`, {
      method: "DELETE",
      auth: true,
    });
  },
};